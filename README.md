# Portafolio Python

Landing page personal construida en Python.

## Ejecutar

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python -m portfolio.dev
```

Abrir:

```text
http://127.0.0.1:8000
```
